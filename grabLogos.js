/**
 * javascript functions for grabbing logos
 */

function hello(name) {
    return `Hello ${name}`;
}
  
console.log(hello("Jeremiah"));

function buildLogoURL(abbreviation){

console.log(`This should be the ESPN url, built with javascript string. ${abbreviation}`);
}

buildLogoURL("JWH");